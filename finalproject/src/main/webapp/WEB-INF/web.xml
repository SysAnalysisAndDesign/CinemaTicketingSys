<!DOCTYPE web-app PUBLIC
 "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
 "http://java.sun.com/dtd/web-app_2_3.dtd" >


<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/
web-app_3_1.xsd"
          version="3.1">

  <display-name>Archetype Created Web Application</display-name>

  <servlet>
    <servlet-name>mvc-dispatcher</servlet-name> <!-- 自定义名字  与xxxx-servlet.xml的xxx对应-->
    <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class> <!-- 使⽤Spring MVC框架的类 -->
    <!--  <init-param>
          <param-name>contextConfigLocation</param-name>
          <param-value>/WEB-INF/mvc-dispatcher-servlet.xml</param-value>
      </init-param> -->
      <load-on-startup>1</load-on-startup>
  </servlet>

  <servlet-mapping> <!--servlet必须有对应的mapping匹配对应url -->
    <servlet-name>mvc-dispatcher</servlet-name>
    <url-pattern>/</url-pattern> <!-- ⽤于拦截请求（url-pattern为 / ，说明拦截所有请求），并交由Spring MVC的后台控制器来处理。这⼀项配置是必须的。 -->
  </servlet-mapping>

    <!-- 为了能够处理中⽂的post请求，配置⼀个encodingFilter（），避免post请求中⽂时出现乱码的情况 -->
    <filter>
        <filter-name>encondingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>

    <filter-mapping>
        <filter-name>encondingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
</web-app>
